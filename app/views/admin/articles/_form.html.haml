= simple_form_for [:admin, @article] do |f|
  = f.input :entry_at
  = f.input :title
  = f.input :content
  = f.input :published
  = f.input :catch_image
  = f.button :submit, class: 'btn btn-primary'

